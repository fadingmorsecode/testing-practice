const calculator = {
  add: (input) => input[0] + input[1],
  subtract: (input) => input[0] - input[1],
  divide: (input) => input[0] / input[1],
  multiply: (input) => input[0] * input[1],
};

export default calculator;
